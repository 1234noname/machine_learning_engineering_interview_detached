# `environments/prod/app/` — AVSA prod app stack (stub)

Stub. [#10](../../../../../issues/completed/010-prod-promotion-gate.md) populates this when the prod promotion gate is built. Singleton.

State at `gs://<prod-project-id>-terraform-state/prod/app/`. Apply only via the `prod-deploy.yml` workflow under GitHub Environments approval — never locally.
