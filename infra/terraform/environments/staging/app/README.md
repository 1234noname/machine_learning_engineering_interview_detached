# `environments/staging/app/` — AVSA staging app stack (stub)

Stub. Track B's first issue and #9 (staging auto-deploy on merge) populate this directory. Singleton (no per-PR namespacing — staging is one stack).

State at `gs://<staging-project-id>-terraform-state/staging/app/`.
